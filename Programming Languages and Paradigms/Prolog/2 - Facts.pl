%Comment like that (yes, weird)

%Have to keep consistency between number of argments in each RELATION of OBJECTS
    
    %2 args
    eat(duck, bread).
    eat(duck, seed).
    eat(duck, worm).
    eat(human, duck). 
    %eat(human). Dont do that

    %1 arg
    strong(human).
    strong(duck).
    strong(worm).
    weak(bread).
    weak(seed).
    %Known as "properties"

    %3 args
    give(human, bread, duck). 
    %Human give bread to duck


